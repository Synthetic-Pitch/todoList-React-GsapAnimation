
import NavBar from "../components/NavBar";

const History = () => {
    return (
        <main className="bg-[#D4A483] min-h-[100dvh] flex flex-col items-center">
            <NavBar/>
        </main>
    );
};

export default History;